{{#if resume.skills.length}}
    <div class="sectionBlock">
        <div class='sectionContent'>
            <span class='sectionName'>TECHNICAL SKILLS</span>
            <div class="blockHeader skills_content">
                {{#each resume.skills}}
                    <div class="skill_item">
                        {{#if name}}<b>{{name}}</b>{{/if}}
                        {{#if keywords.length}}
                            {{#each keywords}}
                                {{.}}{{#if @last}}.{{else}}, {{/if}}
                            {{/each}}
                        {{/if}}
                    </div>
                {{/each}}
            </div>
        </div>
    </div>
{{/if}}