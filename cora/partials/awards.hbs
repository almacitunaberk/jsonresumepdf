{{#if resume.certificates.length}}
<section id="awards">
	<h2><i class="fa fa-trophy left"></i> CERTIFICATES</h2>
	{{#each resume.certificates}}
	<div class="item">
	<h3>{{name}} - {{issuer}}</h3>
	{{#if date}}
	<h4>
		{{date}}
	</h4>
	{{/if}}
	</div>
	{{/each}}
</section>
{{/if}}